import ModalPadding from '@/components/ui/modal/ModalPadding';

type ExportEveryImageModalProps = {
  close: () => void;
};

export default function ExportEveryImageModal({ close }: ExportEveryImageModalProps) {
  return (
    <ModalPadding>
      TODO export every image
    </ModalPadding>
  );
}
